## Examples

To run examples proceed with following steps:

1. Rename .env.example to .env and fill with necessary values

```bash
cp .env.example .env
```

2. Install `tsx`

```bash
npm install -g tsx
```

3. Run specific example

```bash
npx env-cmd tsx transfer-evm-to-evm.ts
```
